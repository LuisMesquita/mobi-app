// ### HELPERS ###
export const requestSuccess = (dispatch, type, payload) => {
  dispatch({
    type,
    payload
  });
};

export const handleResponse = (dispatch, type, payload) => {
  dispatch({
    type,
    payload
  });
};
